import {  MouseEventHandler } from "react";

export interface CardProps {
    title?: string,
    text?: string,
    disabled?: boolean,
    backgroundColor?: string,
    src?: string,
    img_width?: int
};